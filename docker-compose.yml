 version: "3.3"

 services:
   api-students:
     build: ./api-students
     image: angello599/api-students
     ports:
       - 8000:8000
   websimple:
     build: ./websimple
     image: angello599/websimple
     ports:
       - 8080:80
